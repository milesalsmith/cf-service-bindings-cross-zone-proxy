node_modules/
.wrangler/
.dev.vars
dist/
.DS_Store
*.log
# Next.js / OpenNext build outputs (relevant on the nextjs branch but
# also ignored on main so a stale worktree never accidentally lands them
# here).
.next/
.open-next/
next-env.d.ts
cloudflare-env.d.ts
# Local-only wrangler overrides containing real account-specific
# values (KV ids, workers.dev subdomains, account_id). Never commit.
wrangler.local.toml
wrangler.local.jsonc
.env.deploy
